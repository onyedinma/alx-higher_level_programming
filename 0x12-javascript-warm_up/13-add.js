#!/usr/bin/node
// This script defines a function that adds two numbers and exports it.

// Define the add function using an arrow function expression
const add = (a, b) => a + b;

// Export the add function
exports.add = add;
